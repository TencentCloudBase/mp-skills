<view class="page">
  <welcome-card
    data="{{welcomeData}}"
    bind:action="onAction"
    wx:if="{{!loading}}"
  />
  <view wx:if="{{loading}}" class="loading">加载中...</view>

  <view class="footer">
    <text class="footer-text">运行 mp-skills add 安装更多 Skill</text>
  </view>
</view>
