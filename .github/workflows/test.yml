name: Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'
      - run: npm ci
      - run: npm run typecheck
      - run: npm run format:check
      - name: Run tests
        run: |
          echo "=== installer ===" && npx tsx --test test/installer.test.ts
          echo "=== lock-file ===" && npx tsx --test test/lock-file.test.ts
          echo "=== sanitize ===" && npx tsx --test test/sanitize.test.ts
          echo "=== source-parser ===" && npx tsx --test test/source-parser.test.ts
      - run: npm run build
